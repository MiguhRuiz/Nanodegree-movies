# Defintion of the only class that the "app" needs:
class Film():
    def __init__(self, name, poster, videoid):
        self.title = name
        self.poster_url = poster
        self.video_id = videoid
